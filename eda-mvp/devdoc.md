# What the agent does

1. Accept a CSV/Excel (local path or upload/URL).
2. Auto-profile the dataset (schema, quality, distributions, correlations, simple visuals).
3. Produce a human-readable report + chart bundle.
4. Stay “live” to answer follow-up questions and generate new analyses/plots on demand.

# Architecture at a glance


